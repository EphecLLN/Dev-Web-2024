{
  "name": "app",
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"white,blue\" -p \"[{name}]\"  \"cd server && npm run dev\" \"cd client && npm run dev\" "
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
